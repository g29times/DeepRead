针对处理超大文件（如长文档、大型代码库、视频或海量日志）并进行多次交互的场景，建议采用 **“上传 $\rightarrow$ 挂载缓存 $\rightarrow$ 流式对话”** 的标准流程。这种模式能有效规避单次请求的 Token 限制，并大幅降低推理延迟和成本。

以下是通用化的操作指南：

---

### 1. 核心流程架构

该流程主要解决两个痛点：**文件太大发不出去**（File API 解决）和 **重复发送相同大文件太贵/太慢**（Context Caching 解决）。

---

### 2. 第一阶段：文件上传 (File API)

不要将文件内容作为字符串直接放入 `contents` 数组。先将其上传到 Google 的临时存储中。

```python
from google import genai
from google.genai import types

client = genai.Client(api_key="YOUR_API_KEY")

# 上传文件（支持 PDF, 文本, 视频, 音频等）
myfile = client.files.upload(
    file="your_large_data.ext",
    config=types.UploadFileConfig(
        mime_type="text/plain", # 根据实际类型调整，如 application/pdf
        display_name="Project_Context_Data"
    )
)

print(f"文件上传成功，URI: {myfile.uri}")

```

> **注意**：File API 存储的文件通常在 48 小时后自动删除，且不计入推理费用。

---

### 3. 第二阶段：创建上下文缓存 (Context Caching)

如果你打算针对这个文件问多个问题，**必须**创建 Cache。这会将文件预加载到模型的“近端内存”中。

```python
# 定义模型
model_id = "gemini-3-flash-preview"

# 创建缓存
# 缓存通常包含：系统提示词 + 大文件内容
my_cache = client.caches.create(
    model=model_id,
    config=types.CreateCachedContentConfig(
        display_name="resource_heavy_task",
        contents=[myfile], 
        system_instruction="你是一个专业的数据分析助手，请基于提供的参考资料回答问题。",
        ttl="3600s" # 缓存有效期，过期会自动销毁以省钱
    )
)

```

* **起步阈值**：文件大小必须超过 **32,768 tokens** 才能启用缓存。
* **费用说明**：缓存按“存储容量 $\times$ 时间”计费，但后续对话的 **输入 Token 费用通常会有显著折扣**。

---

### 4. 第三阶段：流式对话交互

在生成内容时，不再传入文件对象，而是直接引用 `cache.name`。

```python
# 使用 stream_generate_content 进行流式交互
response_stream = client.models.generate_content_stream(
    model=model_id,
    contents="请总结这份文件的核心大纲。",
    config=types.GenerateContentConfig(
        cached_content=my_cache.name # 关键：指定使用刚才创建的缓存
    )
)

for chunk in response_stream:
    print(chunk.text, end="")

```

---

### 5. 最佳实践建议

* **生命周期管理**：
缓存是按时计费的。如果任务提前结束，建议手动删除缓存以节省成本：
`client.caches.delete(name=my_cache.name)`
* **更新缓存**：
缓存创建后内容是不可变的。如果你增加了新文件，需要创建一个新的 Cache 实例。
* **Token 监控**：
虽然缓存能处理大数据，但依然受限于模型总窗口（如 Flash 的 1M Token）。如果文件本身已经接近 $10^6$ Token，建议在上传前进行必要的物理截断或压缩。

---

### 总结对照表

| 步骤 | 解决的问题 | 核心 API 动作 |
| --- | --- | --- |
| **上传** | 突破 HTTP 请求体大小限制 | `client.files.upload` |
| **缓存** | 减少重复计算，降低长文本输入成本 | `client.caches.create` |
| **生成** | 获取快速、流式的响应 | `generate_content_stream(cached_content=...)` |


---

针对 Google AI Studio (Gemini API) 平台的 Python 封装类。这个脚本涵盖了从**文件上传**、**创建缓存**到**流式提问**以及**清理**的完整生命周期。
根据最新的 Google AI SDK（`@google/genai`）以及针对 **Gemini 3.0 / 3.1** 系列模型的文档，官方推荐使用全新的 **Google Gen AI SDK** 来替代旧版的 `generative-ai`。

在 Gemini 3 系列中，Context Caching 的生效阈值从之前的 32,768 tokens 降低到了 **4,096 tokens**，这对处理几兆大小的日志文件（如你的 HAR 文件）非常友好。

### 核心更新点

1. **新 SDK**: `npm install @google/genai` (注意包名去掉了 `erative`)。
2. **新模型**: 使用 `gemini-3-flash-preview` 或更极致性价比的 `gemini-3.1-flash-lite-preview`。
3. **隐式缓存与显式缓存**: Gemini 3 支持显式创建缓存（Cache API），以便在多轮对话中复用同一个上下文。

---

### 通用文件处理 JS 指南 (Gemini 3 系列)

```javascript
import { GoogleGenAI } from '@google/genai';

/**
 * 针对 Gemini 3.0+ 优化的通用文件处理类
 */
class Gemini3Expert {
  constructor(apiKey, modelId = 'gemini-3-flash-preview') {
    // 初始化新版 SDK
    this.client = new GoogleGenAI({ apiKey });
    this.modelId = modelId;
    this.file = null;
    this.cache = null;
  }

  /**
   * 第一步：使用 File API 上传文件
   */
  async uploadFile(filePath, displayName) {
    console.log(`[File API] 正在上传: ${filePath}...`);
    // 自动识别 MIME 类型或手动指定
    const file = await this.client.files.upload({
      file: filePath,
      config: { displayName }
    });
    this.file = file;
    console.log(`[OK] 文件上传成功: ${file.uri}`);
    return file;
  }

  /**
   * 第二步：创建显式 Context Cache (上下文缓存)
   * Gemini 3 只要超过 4,096 tokens 即可缓存
   */
  async createContextCache(systemInstruction, ttlSeconds = 3600) {
    if (!this.file) throw new Error('请先上传文件');

    console.log(`[Cache API] 正在为模型 ${this.modelId} 创建缓存...`);
    const cache = await this.client.caches.create({
      model: this.modelId,
      config: {
        // 将文件作为基础上下文放入缓存
        contents: [
          {
            parts: [{ fileData: { fileUri: this.file.uri, mimeType: this.file.mimeType } }]
          }
        ],
        systemInstruction: systemInstruction,
        ttlSeconds: ttlSeconds
      }
    });
    this.cache = cache;
    console.log(`[OK] 缓存已就绪: ${cache.name}`);
    return cache;
  }

  /**
   * 第三步：结合流式生成 (generateContentStream) 使用缓存
   */
  async chatWithStream(prompt) {
    if (!this.cache) throw new Error('请先创建缓存');

    console.log('\n[Gemini 3 响应]: ');
    
    // 注意：在新版 SDK 中，通过 config.cached_content 引用缓存
    const responseStream = await this.client.models.generateContentStream({
      model: this.modelId,
      contents: prompt,
      config: {
        cached_content: this.cache.name,
        // Gemini 3 特有：thinking_level 
        // thinking_config: { thinking_level: 'LOW' } // 可选，Lite 模式通常不需要
      }
    });

    for await (const chunk of responseStream) {
      process.stdout.write(chunk.text());
    }
    console.log('\n' + '-'.repeat(40));
  }

  /**
   * 清理缓存（按量计费，建议主动删除）
   */
  async cleanup() {
    if (this.cache) {
      await this.client.caches.delete(this.cache.name);
      console.log(`[Cleanup] 缓存 ${this.cache.name} 已手动释放。`);
    }
  }
}

// ==========================================
// 运行示例
// ==========================================
(async () => {
  const API_KEY = '在此填写你的_AI_STUDIO_API_KEY';
  
  // 备选模型: 'gemini-3.1-flash-lite-preview' (延迟更低，成本更低)
  const expert = new Gemini3Expert(API_KEY, 'gemini-3-flash-preview');

  try {
    // 1. 上传
    await expert.uploadFile('server_logs.har', 'Debug_Data');

    // 2. 缓存
    await expert.createContextCache(
      '你是一个专业的后端调试助手。我会给你一份 HAR 日志，请基于其中的网络请求协助我分析接口报错。'
    );

    // 3. 多轮流式提问（每次提问都秒开，因为 1M tokens 已经在缓存里了）
    await expert.chatWithStream('找出所有返回 500 的接口，并提取其 Response Body 中的错误代码。');
    await expert.chatWithStream('根据上面的报错，如果是 Docker 认证问题，请给出具体的 config.json 修改方案。');

  } catch (err) {
    console.error('运行出错:', err);
  } finally {
    // 4. 清理
    await expert.cleanup();
  }
})();

```

### 为什么在 Gemini 3 下这是最优解？

1. **突破 Token 限制**：你的报错提示输入超过了 `1,048,576`。通过 `files.upload` 和 `caches.create`，你可以将这 100 万 token 托管在服务端，模型在处理时会有专门的预加载逻辑，避免单次 Request 体积过大导致的接口超时或 400 报错。
2. **极速响应**：Gemini 3.1 Flash-Lite 配合 Context Cache，即使面对上百万 token 的上下文，首字生成时间（TTFT）通常也能保持在 **1-2 秒**内。
3. **智能思维（Thinking）**：Gemini 3 系列模型原生支持推理能力。如果你需要更深度的逻辑排查（例如多个 HTTP 请求之间的关联分析），你可以不带参数使用默认配置，模型会自动进行“思维链”推理。
4. **成本优化**：显式缓存的读取费用远低于每次全量发送 1M token 的输入费用。

**环境提醒**：

* 请务必卸载旧版：`npm uninstall @google/generative-ai`
* 安装最新版：`npm install @google/genai`